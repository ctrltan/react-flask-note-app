import { useState } from "react";

const postLogin = async () => {

}

const LoginButton = () => {
    return (
        <button 
            title="Login"
            onClick={postLogin}
        />
    );
}

export default LoginButton;