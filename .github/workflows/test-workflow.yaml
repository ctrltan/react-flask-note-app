name: Run tests

on:
  push:
    branches:
      - '**'

jobs:
  unit-tests:
    runs-on: ubuntu-latest


  integration-tests:
    if: github.event_name == 'pull_request'
    
    runs-on: ubuntu-latest